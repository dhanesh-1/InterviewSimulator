```mermaid
classDiagram
    class User {
        +ObjectId id
        +String name
        +String email
        +String password
        +String role
        +String company
        +Number totalSessions
        +Number averageScore
        +Date createdAt
        +Date updatedAt
        +comparePassword(candidatePassword) Boolean
    }

    class Resume {
        +ObjectId id
        +ObjectId userId
        +String originalName
        +ParsedData parsedData
        +String rawText
        +Date uploadedAt
    }

    class ParsedData {
        +String[] skills
        +Experience[] experience
        +Education[] education
        +String summary
        +String[] certifications
    }

    class Session {
        +ObjectId id
        +ObjectId userId
        +ObjectId resumeId
        +String role
        +String difficulty
        +Question[] questions
        +Number overallScore
        +String status
        +Date startedAt
        +Date completedAt
    }

    class Question {
        +ObjectId id
        +String text
        +String type
        +String difficulty
        +String userAnswer
        +String answeredVia
        +Evaluation evaluation
        +Date answeredAt
        +AnswerVersion[] answerVersions
    }

    class Evaluation {
        +Number relevance
        +Number clarity
        +Number confidence
        +Number overall
        +String feedback
        +String[] suggestions
    }

    class AnswerVersion {
        +ObjectId id
        +Number versionNumber
        +String answer
        +String answeredVia
        +Evaluation evaluation
        +Date answeredAt
    }

    class Job {
        +ObjectId id
        +ObjectId recruiterId
        +String title
        +String company
        +String location
        +String type
        +String description
        +String[] requirements
        +String role
        +String difficulty
        +String status
        +Number applicantCount
    }

    class Application {
        +ObjectId id
        +ObjectId jobId
        +ObjectId candidateId
        +ObjectId resumeId
        +ObjectId sessionId
        +String status
        +Number tabSwitchCount
        +InterviewFlags interviewFlags
        +String recruiterNote
        +Date appliedAt
        +Date completedAt
    }

    class InterviewFlags {
        +Boolean disconnected
        +Date resumedAt
    }

    class OpenAIService {
        <<service>>
        +MODEL: String
        +generateQuestions(parsedResume, role, difficulty, previousPerformance, jobDescription) Question[]
        +evaluateAnswer(question, userAnswer, resumeContext) Evaluation
        +parseResumeWithAI(rawText) ParsedData
        +analyzeATSWithAI(resumeText, jobDescription) ATSAnalysisResult
    }

    class ResumeParserService {
        <<service>>
        +extractText(filePath, originalName) String
        +cleanupFile(filePath) Void
    }

    class AnswerVersioningUtil {
        <<utility>>
        +shouldCreateRevision(params) Boolean
        +getNextVersionNumber(question) Number
    }

    User "1" --> "*" Resume : uploads
    User "1" --> "*" Session : conducts
    User "1" --> "*" Job : creates (recruiter)
    User "1" --> "*" Application : submits (candidate)
    
    Resume "1" --> "*" Session : customizes
    Resume "1" --> "*" Application : used_in
    
    Job "1" --> "*" Application : has
    
    Session "1" --> "*" Question : contains
    Session "1" --> "0..1" Application : linked_to
    
    Question "1" --> "*" AnswerVersion : tracks
    Question "1" --> "1" Evaluation : has
    AnswerVersion "1" --> "1" Evaluation : has
    
    Application "1" --> "1" InterviewFlags : has
    Resume "1" --> "1" ParsedData : contains
```
